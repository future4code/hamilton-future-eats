import React from 'react';
import {connect} from "react-redux"

class PageLogin extends React.Component{

    render(){
        return(
            <div>
                PageLogin
            </div>
        )
    }
}

export default connect (null, null) (PageLogin);