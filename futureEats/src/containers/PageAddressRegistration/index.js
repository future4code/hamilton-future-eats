import React from 'react';
import {connect} from "react-redux"

class PageAddressRegistration extends React.Component{

    render(){
        return(
            <div>
                ATUALIZAR ENDEREÇO
            </div>
        )
    }
}

export default connect (null, null) (PageAddressRegistration);